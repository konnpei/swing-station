# Supabase
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Google Calendar OAuth (never commit real secret values)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# Optional: target shared calendar ID. If omitted, events are created in the signed-in user's primary calendar.
GOOGLE_CALENDAR_ID=
